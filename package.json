{
  "name": "insighta_web_portal",
  "version": "1.0.0",
  "description": "Insighta Labs+ Web Portal - Static HTML",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp public/_redirects dist/_redirects && cp railway.toml dist/railway.toml",
    "start": "npx serve dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serve": "^14.2.0"
  }
}
